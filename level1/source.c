#include <stdlib.h>
#include <stdio.h>

void run(void) {
    fwrite("Good... Wait what?\n", 1, 19, 1)
    system("/bin/sh");
}

int main(void) {
    char buffer[64];

    gets(buffer);    
    return 0;
}
